@model IEnumerable<Product>
  
@{
    <h2> Products List </h2>

    @if (!Model.Any())
    {
          <p> No Products Available </p>
    }else 
    {
    @foreach(var item in Model)
    {
           <p>@item.Name</p>
    }
    }
}
