@model IEnumerable<User> 
   
<h2> User List </h2>
@{
   
        @if (!Model.Any())
        {
            <p> Empty List </p>
        }

        else
        {
            @foreach(var item in Model)
            {
                   <p> @item.Name</p>
            }
        }
            
        }
